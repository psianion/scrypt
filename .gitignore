# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo
coverage/

# Runtime / local state
.scrypt/scrypt.db
.scrypt/scrypt.db-shm
.scrypt/scrypt.db-wal
.scrypt/trash/
.scrypt/config.json
.scrypt/public/
*.log

# Sample vault content — runtime only, never committed
/notes/
/journal/
/data/

# Local dev vault (mounted at /vault in docker; user notes only)
vault/
test-vault/

# Per-machine docker override + ingest source (generated by `scrypt init`)
docker-compose.override.yml
/ingest/

# Environment
.env
.env.local
.env.*.local

# Test output
test-results/
test-screenshots/
.playwright-mcp/
*.png
!assets/screenshots/*.png

# Test scratch dirs. Root cause is now fixed at the source: tests/preload.ts
# redirects TMPDIR to a run-scoped dir outside the repo and deletes it after the
# run (see tests/safe-tmpdir.ts). These patterns stay as a belt-and-suspenders
# safety net for interrupted runs in misconfigured environments.
/scrypt-*/
/bunx-*/
/vault-e2e-*/
/wave8-scenario-*/
/node-compile-cache/

# Editor / IDE
.cursor/
.vscode/
.idea/
.fleet/
.zed/
*.swp
*.swo
*~

# AI assistant state
.claude/
.superpowers/

# OS
.DS_Store
Thumbs.db
ehthumbs.db

# Merge / diff artifacts
*.orig
*.rej

# Misc project dirs kept local only
docs/
.github/

# git worktrees
.worktrees/
